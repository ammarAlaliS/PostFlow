import 'package:flutter/material.dart';

class ThemeColors {

  // Colores para el modo claro
  static const primaryColor = Color(0xFF9AD665); // Color primario
  static const secondaryColor = Color(0xFF2ECC71); // Color secundario
  static const accentColor = Color(0xFFE74C3C); // Color de acento

  // Fondo del modo claro
  static const lightBackgroundColor = Color.fromARGB(255, 245, 236, 228); // Fondo claro

  // Colores de texto en modo claro
  static const primaryTextColor = Color(0xFF333333); // Texto principal
  static const secondaryTextColor = Color(0xFF7F8C8D); // Texto secundario

  // Borde y enlaces en modo claro
  static const borderColor = Color(0xFFE0E0E0); // Color de borde
  static const linkColor = Color(0xFF2980B9); // Color de enlace
  static const regularTextColor = Color(0xFF151515); // Texto regular

  // icon 

  static const iconLight = Colors.white;

  // ==================================================================================

  // Colores para el modo oscuro
  static const darkPrimaryColor = Color(0xFF9AD665); // Color primario (modo oscuro)
  static const darkSecondaryColor = Color(0xFF26A69A); // Color secundario (modo oscuro)
  static const darkAccentColor = Color.fromARGB(255, 253, 20, 3);

  // Fondo del modo oscuro
  static const darkBackgroundColor = Color.fromARGB(255, 23, 23, 23); // Fondo oscuro

  // Colores de texto en modo oscuro
  static const darkPrimaryTextColor = Color(0xffffffff); // Texto principal (modo oscuro)
  static const darkSecondaryTextColor = Color(0xFFB0BEC5); // Texto secundario (modo oscuro)

  // Borde y enlaces en modo oscuro
  static const darkBorderColor = Color(0xFF37474F); // Color de borde (modo oscuro)
  static const darkLinkColor = Color(0xFF82B1FF); // Color de enlace (modo oscuro)
  static const darkRegularTextColor = Color(0xFFF7F7F7); // Texto regular (modo oscuro)

  // icon
  static const iconDark = Colors.black;
}
